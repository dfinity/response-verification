{
  "name": "response-verification",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": "^18",
    "pnpm": "^8",
    "npm": "please use pnpm"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
