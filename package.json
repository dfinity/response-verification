{
  "name": "response-verification",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
