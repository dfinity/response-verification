{
  "name": "http-certification-json-api-tests",
  "private": true,
  "scripts": {
    "test": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@dfinity/response-verification": "workspace:*"
  }
}
